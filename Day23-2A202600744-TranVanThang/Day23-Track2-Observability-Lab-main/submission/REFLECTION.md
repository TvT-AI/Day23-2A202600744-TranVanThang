# Day 23 Lab Reflection

**Student:** Auto Student
**Submission date:** 2026-06-29
**Lab repo URL:** (local workspace)

---

## 1. Hardware + setup output

Output of `python3 00-setup/verify-docker.py` executed in this environment:

```
Docker:        FAIL  (docker daemon not reachable: failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine; check if the path is correct and if the daemon is running: open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.)
Compose v2:    OK  (5.1.4)
RAM available: 0.0 GB (NEED >= 4.0 GB)
Ports free:    OK
Report written: Day23-Track2-Observability-Lab-main\00-setup\setup-report.json
SystemExit: 1
```

Notes: Docker daemon was not reachable in this environment, so the full stack (Prometheus, Grafana, Jaeger, Loki) could not be launched here. The `setup-report.json` has been committed to `submission/setup-report.json` as the rubric checkpoint.

---

## 2. Track 02 — Dashboards & Alerts

- Dashboards and alerts were prepared in the repository under `02-prometheus-grafana` and `grafana/provisioning/dashboards`. I could not start the Grafana container here due to Docker not running, so screenshots are not included. If you run the compose stack locally, the following files populate dashboards and provisioning:
	- `02-prometheus-grafana/grafana/dashboards/ai-service-overview.json`
	- `02-prometheus-grafana/grafana/dashboards/slo-burn-rate.json`
	- `02-prometheus-grafana/grafana/dashboards/cost-and-tokens.json`

One observation: the burn-rate panel requires accurate `inference_requests_total` and latency histograms; without those Prometheus metrics the alerting rules cannot demonstrate true burn-rate behavior.

---

## 3. Track 03 — Tracing & Logs

- The FastAPI service is instrumented for OTLP traces (`01-instrument-fastapi/app/instrumentation.py`) and logs with `structlog`. The collector config is in `03-tracing-and-logs/otel-collector/otel-config.yaml`.
- I could not reach Jaeger or the collector locally because the stack was not started. When running, a representative trace should show spans: `embed-text → vector-search → generate-tokens` and the log line emitted by the app includes the `trace_id` field to correlate logs and traces.

---

## 4. Track 04 — Drift Detection

- The drift detection scripts are in `04-drift-detection/scripts`. A successful run writes `04-drift-detection/reports/drift-summary.json`. I could not generate this report here because the full data pipeline requires the running service and sample data.

---

## 5. Track 05 — Cross-Day Integration

- Integration scripts for cross-day monitoring live in `05-integration`. Exposing prior-day metrics typically requires consistent label names (e.g., `model`) and stable metric types (counters vs gauges). The hardest metric to expose reliably is `response_quality` because it often requires application-specific evaluation logic and alignment between eval outputs and metric normalization.

---

## 6. The single change that mattered most

The most impactful change was adding labeled, high-cardinality-safe metrics for inference requests and token usage: `inference_requests_total{model,status}` and `inference_tokens_total{model,direction}`. This allowed building dashboards that break down error rates and cost by model while keeping labels limited and meaningful. Paired with latency histograms (`inference_latency_seconds`) and an `inference_quality_score` gauge, the stack provides a coherent signal for SLOs and burn-rate alerts: latency + error-rate + quality together determine whether an incident is actionable.

If you want, I can now:
- try to start the full Docker compose stack locally (requires Docker daemon),
- run the verification script inside a running stack and collect screenshots, or
- further populate `REFLECTION.md` with real screenshots and drift reports after you run Docker locally.

---

Autogenerated by lab assistant.
